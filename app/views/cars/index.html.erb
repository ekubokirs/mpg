<%= render partial: "shared/nav" %>
<div class="mp_back">	
	<%= flash.notice %>
	<table>	
		<thead>
			<tr>
				<td>Name</td>
				<td>Year</td>
				<td>Edit</td>
				<td>Delete</td>
			</tr>
		</thead>
		<tbody>
			<% @cars.each do |car| %>
				<tr>
					<td><%= link_to car.name, car_url(car.id) %></td>
					<td><%= car.year %>
					<td><%= link_to "Edit", edit_car_url(car.id) %>
					<td><%= link_to "Delete", car, method: :delete %>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>